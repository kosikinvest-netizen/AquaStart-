[phases.setup]
nixPkgs = ["python311", "wkhtmltopdf"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "python3 pdf_generator.py"
