╔════════════════════════════════════════════════════════════════════════════╗
║                    SUPABASE RLS SETUP - STEP BY STEP                       ║
╚════════════════════════════════════════════════════════════════════════════╝

BROWSER WINDOW SHOULD NOW BE OPEN WITH SUPABASE SQL EDITOR

📋 FOLLOW THESE STEPS:

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Copy SQL content                                                  │
└────────────────────────────────────────────────────────────────────────────┘

In the terminal, run:
  cat SUPABASE_RLS_SETUP.sql | pbcopy     # macOS
  cat SUPABASE_RLS_SETUP.sql | xclip      # Linux
  cat SUPABASE_RLS_SETUP.sql              # View content

Or manually open SUPABASE_RLS_SETUP.sql and copy all (Ctrl+A, Ctrl+C)

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Paste into Supabase SQL Editor                                    │
└────────────────────────────────────────────────────────────────────────────┘

In browser (Supabase SQL Editor):
  1. Click in the editor area
  2. Press Ctrl+A (select all)
  3. Press Ctrl+V (paste SQL)
  4. Verify SQL appears (should be ~200+ lines)

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Execute SQL                                                        │
└────────────────────────────────────────────────────────────────────────────┘

  1. Click "RUN" button (bottom right)
     OR press Ctrl+Enter
  
  2. Wait for execution (3-5 seconds)
  
  3. Check for "Success" message

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 4: Verify Tables Created                                             │
└────────────────────────────────────────────────────────────────────────────┘

In Supabase Dashboard:
  1. Go to Table Editor (left sidebar)
  2. Verify these tables exist:
     ✓ users
     ✓ aquariums
     ✓ water_parameters
     ✓ reports

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 5: Verify RLS Policies                                               │
└────────────────────────────────────────────────────────────────────────────┘

For each table:
  1. Click table name
  2. Click "RLS" tab
  3. Verify policies exist:
     - "Users can read own..."
     - "Users can insert own..."
     - etc.

  4. Verify "RLS enabled" = ON

╔════════════════════════════════════════════════════════════════════════════╗
║                    ✅ EXPECTED RESULT                                      ║
╚════════════════════════════════════════════════════════════════════════════╝

Success output should show:
  ✓ CREATE TABLE (4 times)
  ✓ ALTER TABLE (4 times for RLS)
  ✓ CREATE POLICY (~12+ times)
  ✓ CREATE INDEX (4 times)
  ✓ CREATE FUNCTION (1 time)
  ✓ CREATE TRIGGER (2 times)
  ✓ GRANT (multiple times)

No errors should appear.

╔════════════════════════════════════════════════════════════════════════════╗
║                    🐛 TROUBLESHOOTING                                      ║
╚════════════════════════════════════════════════════════════════════════════╝

ERROR: "relation already exists"
  → Tables already created, skip or drop first:
    DROP TABLE IF EXISTS reports, water_parameters, aquariums, users CASCADE;

ERROR: "permission denied"
  → Check if logged in with correct permissions
  → Use service_role key (not anon key)

ERROR: "syntax error"
  → Check if entire SQL was copied
  → Verify no characters were corrupted

╔════════════════════════════════════════════════════════════════════════════╗
║                    📞 NEED HELP?                                           ║
╚════════════════════════════════════════════════════════════════════════════╝

Return to terminal and type: ./setup_supabase_rls.sh --help
Or check: SUPABASE_README.md

